<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.linmh.webviewtest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.linmh.webviewtest.InitApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher555"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher555"
        android:supportsRtl="true"
        android:theme="@style/Theme.WebViewTest"
        android:usesCleartextTraffic="true">
        <activity android:name=".login.LoginActivity"
            android:theme="@style/Theme.MaterialComponents.Bridge"
            />
        <activity android:name=".FastActivity" />
        <activity
            android:name=".QueryActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".TestActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SettingActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>